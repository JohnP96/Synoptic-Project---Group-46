/*
    Client side javascript
*/

/*function sendAlert() {
    //registers the userdetails storing it in a json file
    
    'end', ()=>{
        if(req.method === 'POST' & req.url === '/register_user'){
            //converting data inside "body" variable to object form
            let user = qs.parse(body);
  
            //converting object to string format for res.end()
            res.end(JSON.stringify(user));
        }
    };
}*/